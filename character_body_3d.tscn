[gd_scene format=3 uid="uid://b8vh7gqrqaqin"]

[sub_resource type="GDScript" id="GDScript_bu0v1"]
script/source = "extends CharacterBody3D

@export var DEGRU = 350
@export var max_pitch = 1.6
@onready var player = $\".\"
@onready var CAM = $camer/Camera3D
const SPEED = 5.0
const JUMP_VELOCITY = 4.5
var gravity: float = ProjectSettings.get_setting(\"physics/3d/default_gravity\")
func _ready():
	Input.set_mouse_mode(Input.MOUSE_MODE_CAPTURED)

func _input(event):
	if event is InputEventMouseMotion:
		player.rotation.y -= event.relative.x / DEGRU
		CAM.rotation.x -= event.relative.y / DEGRU
		CAM.rotation.x = clamp(CAM.rotation.x, -max_pitch, max_pitch)

func _physics_process(delta: float) -> void:
	# Add the gravity.
	if not is_on_floor():
		velocity += get_gravity() * delta

	# Handle jump.
	if Input.is_action_just_pressed(\"ui_accept\") and is_on_floor():
		velocity.y = JUMP_VELOCITY

	# Get the input direction and handle the movement/deceleration.
	# As good practice, you should replace UI actions wwwwith custom gameplay actions.
	var input_dir := Input.get_vector(\"left\", \"right\", \"forward\", \"back\")
	var direction := (transform.basis * Vector3(input_dir.x, 0, input_dir.y)).normalized()
	if direction:
		velocity.x = direction.x * SPEED
		velocity.z = direction.z * SPEED
	else:
		velocity.x = move_toward(velocity.x, 0, SPEED)
		velocity.z = move_toward(velocity.z, 0, SPEED)

	move_and_slide()
"

[sub_resource type="CapsuleShape3D" id="CapsuleShape3D_iwpo4"]

[sub_resource type="CapsuleMesh" id="CapsuleMesh_bu0v1"]

[node name="player" type="CharacterBody3D" unique_id=960941828]
script = SubResource("GDScript_bu0v1")

[node name="CollisionShape3D" type="CollisionShape3D" parent="." unique_id=345799742]
shape = SubResource("CapsuleShape3D_iwpo4")

[node name="camer" type="Node3D" parent="." unique_id=1276813921]

[node name="Camera3D" type="Camera3D" parent="camer" unique_id=1210975621]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.6046821, -0.6166061)

[node name="MeshInstance3D" type="MeshInstance3D" parent="." unique_id=2089664391]
mesh = SubResource("CapsuleMesh_bu0v1")
